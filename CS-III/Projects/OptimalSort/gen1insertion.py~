import sys

def insertion_sort(list):
    i = range(1, len(list))
    for j in i:
        min_value = list[j]

        while list[j-1] > min_value and j > 0:
            list[j], list[j-1] = list[j-1], list[j]
            j = j - 1

    return list

STDIN = []
userInput = sys.stdin

for line in userInput:
    line = line.lower().strip('\n')
    STDIN.append(line)

print(insertion_sort(STDIN))            
